# تقرير إعادة تصميم Landing Page وبوابة المتاجر — Saarly

## ما تم إصلاحه

- إعادة بناء الهيدر ليبقى متزنًا على الكمبيوتر والموبايل من غير تزاحم حول الشعار.
- إعادة تنظيم الـHero بالكامل: مسافات صحيحة، عنوان واضح، أزرار ثابتة، ونموذج هاتف وكروت متحركة بدون تداخل.
- إعادة كتابة أقسام الصفحة الرئيسية وتسلسلها لتوضيح قيمة Saarly للمشتري والمتجر.
- توحيد المقاسات، الحواف، الظلال، الخطوط، المسافات، وحالات الحركة في نظام تصميم واحد.
- إعادة ضبط الوضع الفاتح والغامق والـRTL والـLTR.
- تحسين صفحات السياسات والدعم وحذف الحساب وتسجيل الدخول واستعادة كلمة المرور.
- إعادة تصميم بوابة المتاجر وأقسامها وجداولها ونماذجها لتعمل على الشاشات الصغيرة والكبيرة.
- إضافة صفحات خطأ وتحميل مخصصة بدل تجربة 404 فارغة.
- إضافة تشغيل نظيف يحذف كاش Next القديم لتجنب 404 الناتج عن كاش أو تشغيل من مجلد خاطئ.

## التشغيل المحلي

أسهل طريقة: شغّل الملف:

`START_LOCAL.bat`

أو من PowerShell داخل مجلد `Landing Page`:

```powershell
npm install
npm run dev
```

ثم افتح الرابط الذي يظهر في PowerShell، وهو ثابت محليًا على:

`http://localhost:3100`

## التحقق المنفذ

- فحص تركيب جميع ملفات TypeScript وTSX: ناجح.
- فحص Parsing لملف CSS: ناجح.
- فحص وجود المسارات العامة الأساسية: ناجح.

تعذر تشغيل `next build` داخل بيئة التسليم بسبب انتهاء مهلة الاتصال بـnpm Registry، لذلك يجب تشغيل `npm install` و`npm run build` على الجهاز قبل النشر النهائي.
