# تقرير تعديل اللاندنج بيدج — V6 Hero / OG / SEO Fixes

## ما تم تنفيذه

### 1) تصحيح متغير TikTok
- تم استبدال المتغير القديم `NEXT_PUBLIC_LINKEDIN_URL` بـ `NEXT_PUBLIC_TIKTOK_URL`.
- تم تعديل المصدر في:
  - `src/lib/site-content.ts`
  - `.env.example`

### 2) تحسين الهيرو سيكشن على الكمبيوتر
- تم زيادة المسافة بين بلوك النص وبين الموبايل التوضيحي.
- تم إبقاء نفس الفكرة ونفس الارتفاع العام، مع إعادة تموضع الكروت العائمة بحيث لا تزحف فوق العنوان.
- تم تضييق عرض عمود الموبايل قليلًا مع زيادة `gap` بين العمودين لراحة بصرية أفضل.

### 3) تحسين نسخة الموبايل ومنع الإزاحة الأفقية
- تم ضبط الـ hero visual ليبقى داخل العرض المتاح مع `overflow` مناسب.
- تم تقليل حجم عنصر الموبايل نفسه قليلًا في الشاشات الصغيرة.
- تم تقليل أحجام الكروت العائمة، وتحريكها لتجنب تغطية النصوص.
- تم إخفاء البطاقة الثالثة على الموبايل الصغير كما كان موجودًا، مع تحسين تموضع البطاقتين الظاهرتين.
- الهدف من ذلك منع المشكلة التي كانت تجعل الصفحة تبدو مزاحة يمينًا عند أول تحميل على بعض الأجهزة.

### 4) تحديث صورة المعاينة عند مشاركة الرابط (OG Image)
- تم استبدال صورة المعاينة القديمة بصورة جديدة تعتمد على شعار سعرلي نفسه.
- تم ضبط المقاس على `1200x630`.
- تم مراعاة وجود مسافات آمنة حول اللوجو حتى لا يكون كبيرًا جدًا أو ملاصقًا للحواف.
- الملف المعدل:
  - `public/og-image.png`

### 5) تحسين الميتاداتا وتهيئة SEO
تم تحسين:
- `title`
- `description`
- `keywords`
- `openGraph`
- `twitter cards`
- `icons`
- `apple web app`
- `canonical`
- `alternate languages`
- `robots`
- `sitemap`
- `manifest`

### 6) تنظيف الفهرسة
- تم الإبقاء على الصفحات العامة المفيدة للفهرسة.
- تم منع فهرسة مسارات تسجيل الدخول والبوابات الداخلية والـ API من خلال `robots`، حتى تظل نتائج البحث أنظف وأكثر احترافية.

## الملفات التي تم تعديلها
- `src/lib/site-content.ts`
- `.env.example`
- `src/app/layout.tsx`
- `src/app/robots.ts`
- `src/app/sitemap.ts`
- `src/app/manifest.ts`
- `src/app/globals.css`
- `public/og-image.png`

## ملاحظة التحقق
تم تنفيذ التعديلات مباشرة على النسخة الحالية من مشروع اللاندنج بيدج.
تعذر تشغيل `npm install` وBuild داخل البيئة الحالية بسبب مشكلة Registry خارجية عند تنزيل الحزم، لذلك التحقق هنا كان على مستوى تعديل الكود والأصول والهيكل.

## النسخة النهائية
- مجلد العمل المعدل:
  `/mnt/data/saarly_latest/extracted/web/Landing Page`
- النسخة المضغوطة المعدلة:
  `/mnt/data/Saarly_Web_Landing_V6_Hero_OG_SEO_Fix.zip`
