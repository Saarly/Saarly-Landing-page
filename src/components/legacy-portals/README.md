# Legacy portal snapshot (inactive)

This directory is a preserved snapshot of the Buyer and Merchant portal UI that existed before the 2026-08-09 portal rebuild.

It is intentionally **not imported by active routes**. It remains here for visual/data-flow reference and rollback only.

Do not wire these components back into `/buyer` or `/merchant` unless an explicit rollback is requested.
