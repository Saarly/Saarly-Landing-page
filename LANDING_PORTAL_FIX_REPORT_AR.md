# تقرير إصلاح Landing Page وبوابة المتاجر — Saarly

## الحكم

**النسخة خلصانة على مستوى كود المصدر والوظائف المطلوبة، وجاهزة للبناء والاختبار التشغيلي بعد تطبيق إصلاحات Supabase ووضع متغيرات البيئة.**

لم يتم الادعاء بأنها Production-Certified؛ لأن بيئة الفحص لم تستطع تثبيت حزم npm بسبب استجابة `503` من Registry، وبالتالي تعذر تشغيل `next build` والمتصفح الفعلي هنا.

## الواجهة العامة

تم استبدال النموذج البسيط بواجهة تسويقية كاملة تشمل:

- هوية Saarly والشعار الحقيقي بدل الشعار المؤقت.
- Hero متقدم بتكوين بصري ثلاثي الأبعاد ومؤثرات زجاجية وحركة هادئة.
- أقسام طريقة العمل، مميزات المشتري، مميزات المتجر، الأقسام، الأمان والخصوصية، الأسئلة الشائعة، التحميل وCTA نهائي.
- تصميم عربي وإنجليزي مع RTL/LTR وتبديل اللغة.
- Light/Dark/System مع حفظ اختيار المستخدم.
- استجابة للموبايل والتابلت والكمبيوتر.
- احترام `prefers-reduced-motion`.
- عدم الاعتماد على صور Stock خارجية قد تتوقف.
- استخدام الشعار والأصول المحلية.

## السياسات والدعم

- تمت مطابقة نصوص السياسات مع النصوص المسجلة داخل تطبيق Flutter بدل كتابة سياسات منفصلة متعارضة.
- إضافة تاريخ إصدار واضح للسياسات.
- صفحة دعم ترسل طلبًا حقيقيًا إلى السيرفر.
- صفحة حذف الحساب تنشئ طلب حذف حقيقيًا من البريد المرتبط بالحساب.
- إضافة صفحة استعادة كلمة المرور وصفحة تعيين كلمة المرور الجديدة.
- بنود الاسترداد وحذف البيانات ما زالت تحتاج مراجعة قانونية قبل النشر الرسمي.

## تسجيل الدخول والحماية

- تسجيل دخول البريد وكلمة المرور عبر Supabase Auth.
- التحقق بعد الدخول أن الحساب مرتبط بمتجر أو موظف متجر نشط.
- منع حساب المشتري فقط من دخول البوابة.
- منع المتجر المرفوض أو غير المسموح من الحصول على واجهة تشغيل وهمية.
- التحقق من صلاحية كل قسم في السيرفر، وليس بإخفاء رابط الواجهة فقط.
- Service Role محصور في مسارات السيرفر.

## بوابة إدارة المتجر

الأقسام المنفذة:

1. نظرة عامة ومؤشرات التشغيل.
2. بيانات المتجر والأقسام وطريقة التسعير والتوصيل والحرفي.
3. المنتجات: إضافة، تعديل، صور، تفعيل وإيقاف، واستيراد CSV/XLSX.
4. طلبات التسعير العامة والمخصوصة وإرسال الردود.
5. الطلبات ومراحل التأكيد والإلغاء والتواصل بعد السماح.
6. الفروع ومستندات مدير الفرع.
7. الموظفون والأدوار والصلاحيات ونطاق الفروع.
8. الإشعارات وحالة القراءة.
9. الاشتراك والخطط والفترة المجانية وفترة السماح.
10. طلبات الدفع اليدوي ورفع الإثبات وسجل الطلبات.
11. العمولات والرصيد والتسويات.
12. الإعدادات والدعم وطلب حذف الحساب.

## الأمان الوظيفي

- التحقق من صلاحية الموظف على GET وPOST داخل API.
- منع الموظف من فتح قسم غير مصرح به عن طريق كتابة الرابط يدويًا.
- توحيد مفاتيح الصلاحيات مع تطبيق Flutter.
- رفع المستندات داخل مسار خاص بالمتجر مع التحقق من الملكية والنوع والحجم.
- حساب الأسعار والخصومات والاشتراك من RPCs موثوقة بدل قيم المتصفح.
- عدم اختراع Checkout إلكتروني بدون مقدم دفع فعلي.

## SEO وPWA

- Metadata عامة وصفحات محسنة.
- `robots.ts` و`sitemap.ts` و`manifest.ts`.
- عناوين ووصف للصفحات الأساسية.
- شعار وأيقونة محلية.
- روابط المتاجر وبيانات التواصل قابلة للضبط من متغيرات البيئة.

## الاختبارات المنفذة

- **14 من 14** اختبار أمان ووظائف: ناجحة.
- **49 ملف TypeScript/TSX**: بدون أخطاء تركيبية.
- جميع مسارات الاستيراد المحلية: سليمة.
- فحص عدم وجود Service Role في كود المتصفح: ناجح.

## ما يحتاج تنفيذًا خارجيًا

- وضع متغيرات `.env.local` أو Vercel.
- تشغيل `npm ci` ثم Typecheck/Lint/Build على جهاز متصل بـnpm.
- اختبار المتصفح الفعلي على Desktop/Tablet/Mobile.
- وضع روابط Google Play وApp Store وبيانات التواصل النهائية.
- تركيب مقدم دفع إلكتروني حقيقي إن تقرر تفعيله.
