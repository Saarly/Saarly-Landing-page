# إصلاح TypeScript الخاص باللغة والمظهر

## الخطأ

كان TypeScript يوسّع نوع حالة اللغة من `"ar" | "en"` إلى `string`، ثم يرفض تمريرها إلى `setLocale` التي تقبل نوع `Locale` فقط.

## الإصلاح

- تصدير نوع `ThemeMode` من مزود تفضيلات الموقع.
- تثبيت حالة اللغة في إعدادات المشتري والمتجر كنوع `Locale`.
- تثبيت حالة المظهر كنوع `ThemeMode`.
- تطبيع قيمة المظهر القادمة من قاعدة البيانات قبل إدخالها إلى الحالة.
- رفع رقم الحزمة إلى `1.0.2`.
