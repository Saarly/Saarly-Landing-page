# تحديث Landing Page — مراجعة الهيرو وتجربة الدخول والدعم

هذه النسخة مبنية على `Landing_Page_MERCHANT_SYNCED`، وليست مشروعًا جديدًا منفصلًا.

## التعديلات المنفذة

- إزالة الخلفية والإطار والظل من حاوية شعار Saarly، مع شعار شفاف مناسب للوضعين الفاتح والداكن.
- إزالة عبارة «تصور مبسط لتجربة التطبيق» من رسم الهاتف.
- إعادة بناء عنوان الهيرو في ثلاثة أسطر متوازنة، وجعل «في مكان واحد / in one place» فقط باللون الأخضر، وإزالة الخط الموجود أسفل النص.
- تحويل زر «بوابة المتاجر» إلى «تسجيل دخول للمتجر» وبنفس تصميم زر «شاهد طريقة العمل».
- إعادة بناء «كيف يعمل سعرلي» إلى رحلتين واضحتين: للمشتري وللمتجر، أربع خطوات لكل منهما.
- إضافة تفاعل Hover وحركة خفيفة وكونتراست أقوى لبطاقات الخطوات.
- حذف قسم الأقسام وحذف روابطه من التنقل.
- إزالة الأقسام المتكررة التي كانت تعيد شرح خطوات المشتري بصياغات مختلفة.
- تحسين المسافات وارتفاع السطر للعناوين الكبيرة والنصوص في العربي والإنجليزي.
- تحسين قسم الثقة والأمان والأسئلة الشائعة وكرت تحميل التطبيق.
- تحسين صفحات الخصوصية والشروط والاسترداد وحذف الحساب والدعم بصريًا.
- تحويل دخول المتاجر من بريد وكلمة مرور إلى بريد + رمز OTP من 6 أرقام.
- إضافة «تذكرني على هذا الجهاز» بشكل فعلي: تخزين دائم عند التفعيل، أو جلسة تنتهي بإغلاق المتصفح عند إلغائه.
- نموذج الدعم يعمل فعليًا:
  - المستخدم المسجل يرسل إلى محادثات الدعم المرتبطة بلوحة الإدارة.
  - المستخدم غير المسجل ينشئ تذكرة دعم عامة.
  - يتم وضع تنبيه بريد إلى `info@saarly.app` في قائمة البريد، وتشغيل مرسل البريد فورًا عند ضبط سر الإرسال.

## متغيرات البيئة المطلوبة للدعم

ضع القيم التالية في `.env.local` محليًا، ثم في Environment Variables على Vercel:

```env
NEXT_PUBLIC_SUPPORT_EMAIL=info@saarly.app
SUPPORT_NOTIFICATION_EMAIL=info@saarly.app
EMAIL_DISPATCH_SECRET=نفس_السر_الموجود_في_Supabase_Edge_Functions
```

يجب أن يظل `EMAIL_DISPATCH_SECRET` و`SUPABASE_SERVICE_ROLE_KEY` بدون بادئة `NEXT_PUBLIC_`.

## التحقق المنفذ

- 20 اختبار Source-level نجح.
- فحص تركيب 50 ملف TypeScript/TSX نجح.
- لم يكتمل `npm ci` داخل بيئة التسليم بسبب مهلة مستودع الحزم؛ شغّل البناء على جهازك.

## التشغيل

```powershell
npm ci
npm run typecheck
npm test
npm run build
npm run dev
```

الرابط المحلي:

```text
http://localhost:3100
```
